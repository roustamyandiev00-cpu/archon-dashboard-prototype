import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.archon.dashboard',
  appName: 'Archon Dashboard',
  webDir: 'dist/client'
};

export default config;
